import { PurchasedLineItem } from './purchased-line-item';

describe('PurchasedLineItem', () => {
  it('should create an instance', () => {
    expect(new PurchasedLineItem()).toBeTruthy();
  });
});
